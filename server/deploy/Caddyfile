# 域名在 deploy/.env 注入。Let's Encrypt 证书自动申请续期（ISRG Root X1，ESP32 友好）。
# 备案完成前的联调：临时把站点地址改成 :80 并用 IP+端口访问（docs/00 §2）。

{$API_DOMAIN} {
	reverse_proxy api:3000
}

{$APP_DOMAIN} {
	root * /srv/web
	encode zstd gzip
	file_server
}
