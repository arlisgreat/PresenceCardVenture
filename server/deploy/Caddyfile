# 域名由 deploy/.env 注入；Caddy 管理 HTTPS。

{$API_DOMAIN} {
	reverse_proxy api:3000
}

{$APP_DOMAIN} {
	encode zstd gzip
	handle /v1/* {
		reverse_proxy api:3000
	}
	handle {
		root * /srv/web
		try_files {path} /index.html
		file_server
	}
}
