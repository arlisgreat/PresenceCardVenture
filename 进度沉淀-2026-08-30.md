# 进度沉淀 · 2026-08-30（相框 / Web UI / 演示动效）

> 负责人：我（相框端 + Web UI + 动效演示）；小卡（CoreS3 相机端）由队友开发。
> 本文件是个人工作沉淀，未提交 git。

---

## 1. 当前状态一句话

仓库已全量读完、分支已理清、相框客户端代码已逐行读完（777 行），**开发环境尚未搭建、板子尚未烧录**。下一步从"连通相框"开始。

## 2. 分支结论

| 分支 | 结论 |
| :--- | :--- |
| `codex/hardware` | **开发基点**，最新固件（08-29/30 推进：CJK 字体、堆检查等） |
| `codex/display-to-web` | 相框快照，**无独有提交**，display 代码与 hardware 头部完全一致 |
| `codex/core-to-web` | 小卡自拍 UI 快照，同样无独有提交（队友的线） |
| `codex/algorithm` | **联调 demo server**（免数据库，种子用户 ayan/momo/luna，token demo-token） |
| `main` | 契约基线，落后 hardware 约 29 个提交 |

⚠️ 不要基于快照分支开工，从 `codex/hardware` 头部拉工作分支。

## 3. 相框硬件与文档（已确认）

- **板子：Sunton ESP32-8048S043C**，4.3 寸 800×480 IPS，16 线并行 RGB565 直驱
- ESP32-S3 + 16MB Flash（QIO）+ 8MB OPI PSRAM（固件开机校验，<7MB 拒跑）
- 触摸 **GT911**（板载，代码刻意未初始化）；背光 GPIO2 PWM；PCLK 保守 12.5MHz
- ⚠️ **烧录波特率必须 115200**（CH340 高波特率丢包，注释有记录）
- 文档位置：`firmware/display/README.md`、`include/DisplayBoard.h`（全部引脚）、`boards/esp32-8048S043C.json`、`tools/configure.py`、`src/main.cpp`（全部逻辑）

## 4. 相框代码现状（main.cpp 通读结论)

已实现：领 6 位配对码 → 3s 轮询 `/pair/status` 拿 token 存 NVS → 每 10s 轮询 `/device/state` + `/feed?limit=1` → 下载最新照片（同源限制、≤160KB、必须 320×240 JPEG、SOI/EOI/SOF 校验）→ 2.5× 放大上下各裁 60px 铺满 800×480。

局限（= 迭代方向）：
1. **任何 401 都 clearBinding() 解绑重配对** —— server 生产模式 `/device/state` 有已知 401 bug，不改会反复自我解绑（雷，最优先修）
2. WiFi/API 编译期写死（configure.py 生成 device_config.h），换网要重烧
3. 只显示最新 1 张，无轮播无历史
4. 触摸未初始化，纯显示
5. 新照片硬切，无到达动效
6. 无 OTA（升级插 USB）

## 5. 已知契约缺口（server 侧的债，提给全栈，自己只做容错）

1. `POST /v1/reactions` 不认设备 token（设备点赞恒 401）
2. server 偶发 1×1 占位图（固件门禁拒收 → 表现为"不更新"）
3. Prisma 生产模式 `/device/state` 401
4. 滤镜 id 两侧命名不一致（web: none/warm/vivid/bw/film vs 固件: original/warm/cool/bw/vintage）
5. 无云端 dev 环境，只能局域网联调（`PVC_API_BASE` 指本机 IP，IP 变要重烧）

## 6. 工作规划（三样东西，一套 token 喂三个出口）

**第 1 步：连通相框（半天，纯执行）**
worktree 检出 `codex/algorithm` → `server/api npm ci && npm run dev`（:3000）→ `firmware/display/tools/configure.py`（WiFi + `http://<本机IP>:3000/v1` + `PVC_ALLOW_HTTP_LOCAL_DEV=1`）→ 烧录（115200，**不要全盘擦除**）→ 屏显配对码 → web 输码 → 好友传照片上屏。

**第 2 步：HTML 动效原型页（1 天，全局杠杆）**
800×480 网页，七色 token + 规范 §4 动效曲线，演完整故事：待机 → 照片飘入 → 昵称贴纸 → 点星星。
一鱼三吃：录屏 = 演示视频（需求 3 完成）；Web UI 视觉基准（需求 2 对齐物）；相框动效设计稿（需求 1 移植蓝本）。

**第 3 步：相框迭代（主体时间）**
固定顺序：401 解绑加固 → GT911 触摸初始化（点赞/切信息栏）→ 到达动效（照原型搬）→ 多张轮播。每项真机验证。

**Web UI 并行**：唯一对齐点是用第 2 步的 token 与动效参数。

## 7. 配色方案（量化规范七色 → 相框角色分配）

| Token | Hex | 相框用途 |
| :--- | :--- | :--- |
| paper_white | `#FDFBF5` | 全局底色 + 拍立得白框 |
| lilac_purple | `#C7B8EE` | 昵称贴纸底（-2° 旋转 + 1px 白边 + 2px 硬阴影） |
| cream_yellow | `#F5D76E` | 点赞星星、到达粒子 |
| candy_pink | `#F7A8C8` | 点赞高亮、配对成功强调 |
| accent_sky_blue | `#2F7DE0` | 配对码大字、时间戳 |
| grass_green | `#3E7A3A` | 配对/设置面板主题底（照片区不用） |
| dark_night | `#1A1D1A` | 夜间模式底 + 60% 遮罩 |

三原则：**照片全屏时零 UI**（色彩只在配对页/到达瞬间/点赞反馈出现）；**动效 HTML 定稿再进 C++**（相框淡入用背光 PWM 渐变实现，别做全屏 alpha 混合）；**接口问题不自己扛**。

## 8. 文档动作记录

- 微信收到的三份团队文档（UI 量化规范 / 硬件交互与商业故事 / README）已于今日增补"研发落地现状"章节（在 `D:\xwechat_files\...\2026-08\`，微信只读属性已去除）。设想稿与实际契约的差异均已写入增补章节。
- 待办：把增补后的文档搬进仓库 `docs/`，与代码同源。

## 9. 动效原型页交付记录（§6 第 2 步，v1 同日完成，v2 重构后取代）

**v2 定稿（当前有效）：`docs/07-frame-motion-prototype.html` + 规格 `docs/07-frame-motion-spec.md`**

- **34.8s 六幕完整故事**：配对(00.0，grass 面板 + 配对码 482913 逐位弹入 + /pair/code 旁注) → 绑定成功(04.2，整屏闪切 dark_night 底 + candy_pink 对勾) → 待机(06.2) → 照片飘入(09.0) → 铺满全屏/零 UI(13.0) → **GT911 触点涟漪 + 点星星**(17.0) → **第二张压上来轮播**(22.4) → **自动夜间遮罩**(30.0) → 循环(34.8)。
- **v1（12.2s 三幕）被推翻重做的原因**（复盘见 spec §1）：grass_green 没用上、缺配对幕、点星星无触发来源、无轮播节拍、夜间只有手动按钮、节拍表缺固件移植列、无验收清单。
- v2 关键机制：拍立得状态类挂在元素自身（支持两张独立轮播、新的压旧的）；节拍表加「固件移植要点」列（背光 PWM、帧缓冲复用、触摸即画不等回包）；spec 附验收清单与录屏指南。
- 录屏：完整故事 00.0–34.8s；只要"相框日常" 06.2–34.8s。URL `#t=17000` 跳节拍，`,freeze` 定格截图。
- 曲线仍是占位值，待 §4 定稿替换。Web 视觉方向经确认：**七色千禧/CCD 贴纸风**（web 端、固件端、原型三方统一）。
- 验证：9 张关键帧 headless 定格截图 + 逐幕视觉审查，共 5 轮（详见 spec §7 验证轮次记录）。最终定稿的定格模型：hard 模式下 `.noanim` 全程保留、终态由 hardfreeze 规则按类组合钉死——`.noanim` 移除瞬间过渡从旧值起跑、动画复活，headless 捕获的是起跑帧（配对帧数字缺失、绑定帧对勾空白都由此而来）。绑定成功幕因此从"面板上叠对勾"重设计为整屏替换——叠层需要两层同步透明度过渡，正是定格模式死穴；整屏替换后 live 与 freeze 行为天然一致。

**副产物：`docs/06-motion-prototype.html`** —— 小卡↔云端四场景 API 循环（配对绑定/拍照上传/好友下发/按键点赞），报文与时序照 `docs/02-device-api-v1.md` 编写。给队友的小卡线当交互参考，留档。

## 10. 队友交付文档到手（Presence小卡-开发交付总文档-v1.1.md）

微信收到（2026-08，尚未搬进仓库 `docs/`）。通读结论——**两条线的节拍意外对齐**，可直接互相引用：

- **手势表可对齐**：小卡 Tap/连击 = 散开 1 颗 ⭐ + 发 like API（可 2s 聚合批量上报）；长按 1.5s 呼出三模式选择盘；下滑拍照、上滑系统页。相框端 GT911 点赞节拍（17.0s 幕）与其一致，固件第 3 步触摸初始化照此实现。
- **动效参数是现成的**：小卡 §8.3 动效总表给了定稿 easing/时长（闪现飘入 `cubic-bezier(0.25,1,0.5,1)` 500ms、星星粒子 ease-out 1000ms Y0→-40 Scale0.8→1.4、息屏 ease-in 800ms 背光渐暗）——spec §6 开放问题 1「§4 曲线未定稿」可以由这份表回填，相框原型曲线的占位值换成同族参数。
- **七色 token 完全一致**（§8.1），贴纸样式参数更细（昵称贴纸 Top/Left 8px、α0.85、R4、字号 10px、-2°），移植时照抄。
- **息屏/夜间哲学一致**：小卡息屏期新照片静默入库、绝不打扰，唤醒后最新一张先闪现 20s；相框 30.0s 夜间遮罩（背光 PWM 40%）是同一思路的低配版。
- 差异点（不阻塞）：小卡 320×240 竖屏、无侧键纯触摸、WS 推送（5s 轮询兜底）；相框 800×480 横屏、GT911、10s 轮询。API 契约两边都指向 `docs/02-device-api-v1.md`，v1.1 §9 的 7 条接口与 02 文档需要在联调前对一遍（滤镜 id、reactions 401 是已知缺口）。

---

## 追加沉淀 · 2026-08-30 晚：Web 社区功能 + 七色千禧 UI 全部落地

分支 `feature/web-community`（pvc-server worktree），commit `a01c5d5`，15 文件 +1426/-239。

### 已完成
- **Server**：圈子实体（small/big）+ 大圈订阅门控（只认订阅不认好友）+ 策展人种子内容（c_sky/c_film/c_desk）+ 设备混流（好友 24h 静默时混入大圈精选 ≤10）+ 完整账号体系（注册/登录/登出，scrypt 密码，72h session，好友码注册即双向好友）+ `X-Circle-Id` 发帖校验 + `?circle_id=` feed 过滤。api **84/84** 测试绿。
- **Web**：大圈视图（发现/加入/退出/创建/圈内流）、feed tab 服务端过滤、CreateView 真实圈子发布、AuthView 登录注册（?join= 预填好友码）、侧栏真实账号+退出。web **37/37** 测试绿，vite build 干净。
- **UI**：styles.css 全面重写为七色千禧贴纸风——规范七色 token、DotGothic16 像素标题、2px 深色描边+硬阴影、拍立得白框照片卡、lilac 昵称贴纸（-2° 旋转+白边）。
- **动效**（对照硬件规范 §8.3）：照片错峰飘入 500ms、双击星星粒子爆炸（7 颗，ease-out 1000ms，Y0→-40px，scale .8→1.4）、页面转场阻尼回弹、发布"已浮现"仪式、prefers-reduced-motion 全量兜底。
- **契约文档**：docs/02 §4.1 账号 + §4.2 圈子；docs/03 openapi 新增 auth/circles 路径与 AuthResult/Circle schema，feed 补 circle_id/mode 参数。

### 端到端实测（demo server :3100）
注册带好友码→双向好友 ✓；未订阅大圈 403 ✓；订阅后圈内流+图片可访问 ✓；发帖到大圈盖章 circle_id ✓；未订阅发帖 403 ✓；`circle_id=c_small` 只回小圈照片 ✓；`mode=device` 混流 ✓；登出吊销 token 401 ✓。

### 修过的坑
- 种子精选照片（p_cur_*）不在 PhotoStore 的 demo fallback 里，图片端点 503——已加 `isSeededDemoPhoto`（p_demo_/p_cur_ 前缀）并补测试。
- `photo_count` 原来只数 curatedPhotoIds，用户发帖不计数——改为全量扫 circleId。
- `GET /feed?circle_id=c_small` 服务端没过滤（E2E 抓出）——feed 闭包补 circleIdFilter。

### 待办
- PR：`feature/web-community` → `codex/algorithm`（本地已提交，未 push）
- v1.1 交付文档 §9 的 WS 推送、`signal/like` tap_count 聚合、batch_upload 与现有 REST 契约的对齐，留给固件联调期
